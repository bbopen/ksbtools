# $Id: Distfile,v 1.1 1997/11/29 18:06:28 ksb Exp $
#
# N.B. -o nodescend must be set for this Distfile in the active makefile
`( . ) -> ( 'HOST` )
	install ${INTO};

( README INSTALL bin lib etc ) -> ( 'HOST` )
	install -w ${INTO};

( @Make.host@ ) -> ( 'HOST` )
	install -b ${INTO}/Makefile;
'dnl
