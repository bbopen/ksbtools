# $Id: Distfile,v 3.1 1997/11/16 20:38:03 ksb Exp $
#
`( . ) -> ( 'HOST` )
	install ${INTO};

( bin lib etc README ) -> ( 'HOST` )
	install -w ${INTO} ;

( @Make.host@ ) -> ( 'HOST` )
	install -b ${INTO}/Makefile ;
'dnl
