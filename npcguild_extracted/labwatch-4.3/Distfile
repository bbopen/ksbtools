# $Id: Distfile,v 4.1 1997/11/16 20:52:29 ksb Exp $
# N.B. -o nodescend must be set in this Distfile
`( . ) -> ( 'HOST` )
	install ${INTO};

( README INSTALL bin doc lib etc ) -> ( 'HOST` )
	install -w ${INTO};

( @Make.host@ ) -> ( 'HOST` )
	install -b ${INTO}/Makefile;
'dnl
