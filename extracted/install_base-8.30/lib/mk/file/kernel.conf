; config input								(ksb)
; $Id: kernel.conf,v 5.0 1996/09/09 20:59:59 kb207252 Exp $

; we might have to cd to a compile directory here (FREEBSD)?

$Compile(*):	%b %o -m Config %f && make -f config.mk

$Config(*):	config %f

$Info(*):	${echo-echo} 'kernel config input'
