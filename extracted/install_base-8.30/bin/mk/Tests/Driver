#!/bin/sh
# $Id: Driver,v 1.5 2007/02/28 20:20:04 ksb Exp $
# $Compile: ./%f && echo OK
# This

unset MK
for REGR in ExitCodes options params typenot compat file.x,y \
	goto lines markers table RE here dicer step
do
	mk $REGR || exit 1
done
mk -a -mTempl -e ./template Driver || exit 1

exit 0
